typedef enum{
    OK,
    NOK
} initStatus;

typedef enum{
    OK,
    NOK
} functionStatus;

typedef enum{
    OK,
    NOK
} updateStatus;

typedef enum{
    none,
    takePhoto,
    goToSleep,
    startStream
} deviceAction;